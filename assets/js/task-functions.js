 $(document).ready(function(){
  $("task-color-button").click(function(){

$(function () {
    $('task-color-button').click(function(){
        if ($(this).hasClass('task-color-button-selected') {
                $(this).removeClass('task-color-button-selected');
                //Insert logic if you want a type of optional click/off click code
            } 
            else
            {
                $(this).addClass('task-color-button-selected');
                //Insert event handling logic
            }
    })
});